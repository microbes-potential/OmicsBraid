% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classification.R
\name{classify_braids}
\alias{classify_braids}
\title{Classify cross-omic braid patterns using inferential evidence}
\usage{
classify_braids(
  equivalence,
  omic_order,
  covariance = NULL,
  integrated = NULL,
  trend = NULL,
  trajectory_margin = 0.15,
  alpha = 0.05,
  min_slope = NULL
)
}
\arguments{
\item{equivalence}{Output of `test_equivalence()`.}

\item{omic_order}{Ordered character vector describing the biological/display order of omics.}

\item{covariance}{Optional covariance object used when a trend table must be
computed internally.}

\item{integrated}{Optional output of `integrate_effects()`. Supplying it allows
`no_detectable_effect` to be distinguished from generic uncertainty.}

\item{trend}{Optional output of `test_braid_trend()`. If absent it is computed.}

\item{trajectory_margin}{Smallest meaningful effect change per one-layer
transition for attenuation/amplification.}

\item{alpha}{Local inferential significance level.}

\item{min_slope}{Deprecated alias for `trajectory_margin` retained for early
OmicsBraid prototypes.}
}
\value{
One row per entity containing confirmatory and suggestive labels plus
  trajectory diagnostics.
}
\description{
Braid labels are deliberately conservative. Opposite statistically supported
layer directions confirm inversion. Buffering and emergence require practical
equivalence in the appropriate downstream/upstream layers. Concordance,
attenuation, and amplification require all observed layers to support one
direction and use a covariance-aware GLS trajectory test. If the joint-null
omnibus test is not rejected and practical equivalence is not established,
the result is labelled `no_detectable_effect` rather than incorrectly claiming
equivalence.
}
\details{
A separate `suggestive_pattern` is derived from effect geometry only. It can
be useful when the inferential pattern is unresolved because the data are too
imprecise, but it is not a confirmatory conclusion.
}
